# 使い分けたいユーザー情報と、リポジトリのURL情報を書いてください。
#
# 変更したら、次のコマンドを打ってcommit対象から外しましょう。
# "git update-index --skip-worktree git/gitconfig.local.symlink"
# （commitしてあるファイルは.gitignoreに書いてもcommit対象になります）

[user]
        name = Yoshio HANAWA
[user "ssh://github.com/"]
        email = foo@example.com
[user "https://github.com/"]
        email = foo@example.com
[user "ssh://ghe.example.com/"]
        email = bar@ghe.example.com
[user "https://ghe.example.com/"]
        email = bar@ghe.example.com
